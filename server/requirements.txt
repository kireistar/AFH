fastapi>=0.110.0
uvicorn>=0.27.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# --- Auth (AI concentration scope) ---
# Password hashing standar (bcrypt). BUKAN SHA-256 chained ledger (itu ranah cyber-1).
passlib[bcrypt]>=1.7.4
# JWT access token untuk session (RFC 7519). BUKAN Ed25519 handover signing (itu ranah cyber-2).
python-jose[cryptography]>=3.3.0
# Wajib untuk parsing OAuth2 form data (dipakai FastAPI OAuth2PasswordRequestForm).
python-multipart>=0.0.9
# Validasi format email di Pydantic (EmailStr).
email-validator>=2.1.0